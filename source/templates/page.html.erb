<% content_for(:head) do %>
  <%= dato_meta_tags(page) %>
<% end %>

<%= partial "partials/generic_content", locals: {heading: page.heading, content: page.content} %>

<% if page_partial(page) %>
  <%= partial "partials/#{page_partial(page)}" %> 
<% end %> 
